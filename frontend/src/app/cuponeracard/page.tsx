/**
 * /cuponeracard — la puerta de entrada con el nombre nuevo (2026-09-01).
 *
 * Reexporta la misma landing que /livingcard, que se mantiene viva: esa URL
 * está en material impreso y en redes desde agosto, y romperla dejaría sin
 * destino a folletos ya repartidos.
 */
export { default } from '../cuponera/page';
