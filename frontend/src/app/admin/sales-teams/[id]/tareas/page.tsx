'use client';

import { TareasDelEquipo } from '@/components/ventas/TareasDelEquipo';

/** «Tareas del CRM» del equipo. Todo vive en el componente. */
export default function TareasDelEquipoPage() {
  return <TareasDelEquipo />;
}
