'use client';
import { ListaDeLeads } from '@/components/ventas/ListaDeLeads';

/** Contactos: todas las personas del equipo. */
export default function ContactosPage() {
  return <ListaDeLeads filtro="contactos" />;
}
