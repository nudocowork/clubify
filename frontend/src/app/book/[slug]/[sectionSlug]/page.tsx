// Deep-link a una sección del menú libro (/book/<slug>/<sectionSlug>).
// El sectionSlug lo lee BookClient con useParams y lo pasa al
// MenuBookViewer como initialSectionSlug.
export { default } from '../book-client';
