'use client';

import Pagina from '@/app/admin/sales-teams/[id]/colaboradores/page';

/** Los Colaboradores del equipo, la misma pantalla que ve el admin (ver el layout de /affiliate/equipos/[id]). */
export default Pagina;
