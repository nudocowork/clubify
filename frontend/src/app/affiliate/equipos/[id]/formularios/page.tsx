'use client';

import Pagina from '@/app/admin/sales-teams/[id]/formularios/page';

/** Los Formularios del equipo, la misma pantalla que ve el admin (ver el layout de /affiliate/equipos/[id]). */
export default Pagina;
