// /m/[slug]/delivery — vista DELIVERY del menú público (separación
// definitiva 2026-06-06). Comparte componente con la ruta hermana
// /m/[slug] (MESA); el mode se deriva del path en el cliente.
export { default } from '../storefront-client';
