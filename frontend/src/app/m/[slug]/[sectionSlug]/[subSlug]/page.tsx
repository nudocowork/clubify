// /m/[slug]/[sectionSlug]/[subSlug] — deep-link a una subcategoría
// específica del menú público. Reusa el mismo componente que las rutas
// /m/[slug] y /m/[slug]/[sectionSlug]; lee los 3 slugs via useParams.
export { default } from '../../storefront-client';
