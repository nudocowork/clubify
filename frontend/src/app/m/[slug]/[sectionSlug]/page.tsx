// /m/[slug]/[sectionSlug] — URL compartible que abre directamente una
// sección puntual del menú público. El componente se comparte con la
// ruta hermana /m/[slug]; lee `sectionSlug` directo de useParams.
export { default } from '../storefront-client';
