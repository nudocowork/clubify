{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "cd frontend && npm install && npm run build",
  "installCommand": "echo 'install se hace dentro de frontend/'",
  "outputDirectory": "frontend/.next",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD -- frontend/ package.json"
}
