{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "echo '>>> diag: node=' $(node --version) ' pwd=' $(pwd) ; echo '>>> ls dist:' ; ls -la dist 2>&1 | head -20 ; echo '>>> head dist/main.js:' ; head -3 dist/main.js 2>&1 ; echo '>>> starting nest (no migrate)' ; node dist/main.js 2>&1 ; echo '>>> node EXITED with status' $?",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 600,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
